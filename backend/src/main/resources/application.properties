# ======================================================
# 🔧 SERVER CONFIGURATION
# ======================================================
server.port=8081

# ======================================================
# 🗄️ DATABASE CONFIGURATION (MySQL)
# ======================================================
spring.datasource.url=jdbc:mysql://localhost:3306/user_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ======================================================
# 🔐 SECURITY / JWT CONFIGURATION
# ======================================================
security.jwt.secret=Tr0Xy9nLX8kf7W3uTnZ0jP8A5lKhbGH2eE8wJ9OTq3U
security.jwt.expiration=3600000


# ======================================================
# 📧 EMAIL CONFIGURATION (Optional - use env variables in prod)
# ======================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Added sensible defaults to prevent crashes if environment variables are empty
spring.mail.username=wcvfw2019@gmail.com
spring.mail.password=rfoe vngl gsvz pawq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.test-connection=false

# ======================================================
# 💳 RAZORPAY CONFIGURATION (Optional)
# ======================================================
razorpay.key_id=${RAZORPAY_KEY_ID:rzp_test_XXXXXXXXXXXXXXXXXX}
razorpay.key_secret=${RAZORPAY_KEY_SECRET:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX}
razorpay.webhook_secret=${RAZORPAY_WEBHOOK_SECRET:some_secret}

# ======================================================
# ☁️ AWS S3 (Crucial fix for S3StorageService failure)
# ======================================================
# Using standard Spring Cloud AWS property names and added defaults to prevent NPE
# AWS Configuration (for testing / local development)
cloud.aws.region.static=us-east-1
cloud.aws.credentials.access-key=TESTACCESSKEY123456
cloud.aws.credentials.secret-key=TESTSECRETKEY1234567890
s3.bucket.name=test-financial-documents

# aws.s3.endpoint=${AWS_S3_ENDPOINT:}

# ======================================================
# 📱 Twilio (Optional for SMS OTP)
# ======================================================
twilio.accountSid=${TWILIO_ACCOUNT_SID:ACXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX}
twilio.authToken=${TWILIO_AUTH_TOKEN:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX}
twilio.fromPhone=${TWILIO_FROM_PHONE:+15017122661}

# ======================================================
# 🩺 ACTUATOR / MANAGEMENT ENDPOINTS
# ======================================================
# Expanded exposure to include env, metrics, loggers, mappings for better debugging
management.endpoints.web.exposure.include=health,info,env,metrics,loggers,mappings
management.endpoint.health.show-details=always
debug=true
